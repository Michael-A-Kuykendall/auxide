/target
*.tar.gz
.docs/
.vscode/
.mcp/*.db
.mcp/chroma_db/
.mcp/chroma_index_state.json
.mcp/brain-index/
.mcp/sqlite3.exe
.aider*
PowerShellEditorServices.json
opencode-session-*.md

# Beads: keep the JSONL export durable (recoverable via `bd` auto-import from a newer
# JSONL); the live daemon db and socket are local state and stay ignored.
.beads/beads.db
.beads/bd.sock
!.beads/issues.jsonl

# Regenerable demo artifact (rebuilt by examples/rompler_demo.rs); proof.wav is
# the committed reference, this one is not.
/rompler_demo.wav
